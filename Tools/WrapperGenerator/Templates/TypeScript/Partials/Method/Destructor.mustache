delete(): void {
    if (this.ownsPointer && !this.disposed) {
        registry.unregister(this);
        Module.ccall(
            "{{ unique_name }}",
            "void",
            [ "number" ],
            [ this.pointer ]
        );

        this.disposed = true;
    }
}
