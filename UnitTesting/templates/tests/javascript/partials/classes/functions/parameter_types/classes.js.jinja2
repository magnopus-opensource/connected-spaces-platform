{% for x in ['Value', 'Pointer', 'ConstPointer', 'Reference', 'ConstReference'] %}
    // Pass class by {{ x }}
    test('classFunctions_MyClass_Pass{{ x }}', async function() {
        const instance = dummy.ClassParameterTestClass.create();
        const param = dummy.MyClass.create_value({{ config.class_int_value_first }});

        try {
            instance.classFunctions_MyClass_Pass{{ x }}(param);
        }
        catch (e) {
            assert.fail(e.message)
        }
    });
{% endfor %}